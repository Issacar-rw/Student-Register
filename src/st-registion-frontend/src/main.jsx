import React from 'react';
import ReactDOM from 'react-dom/client';
import StudentRegistration from './App';

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <StudentRegistration />
  </React.StrictMode>
);